# Local Development Environment Variables with AWS SES

# Django Settings
SECRET_KEY=your-local-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Settings (Local MySQL)
DB_NAME=vehicle_parts
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306

# Email Settings - AWS SES (for testing)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=email-smtp.ap-southeast-2.amazonaws.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=AKIASCRDKW6LMO3EDX57
EMAIL_HOST_PASSWORD=4KKUNby5FNClDBompEml9d7pHLgZZk2V4N/WUSH9
DEFAULT_FROM_EMAIL=noreply@vehicleparts.com

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
