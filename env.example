# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# MySQL Database Settings
DB_NAME=vehicle_parts
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_HOST=localhost
DB_PORT=3306

# Email Settings - AWS SES Configuration (ap-southeast-2)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=email-smtp.ap-southeast-2.amazonaws.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=AKIASCRDKW6LMO3EDX57
EMAIL_HOST_PASSWORD=4KKUNby5FNClDBompEml9d7pHLgZZk2V4N/WUSH9
DEFAULT_FROM_EMAIL=noreply@vehicleparts.com

# Alternative: Gmail SMTP (for immediate testing)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
# DEFAULT_FROM_EMAIL=noreply@vehicleparts.com

# AWS S3 Configuration
USE_S3=False
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_STORAGE_BUCKET_NAME=your-s3-bucket-name
AWS_S3_REGION_NAME=us-east-1

# AWS CloudWatch Logs Configuration
USE_CLOUDWATCH_LOGS=False
AWS_CLOUDWATCH_LOG_GROUP=vehicle-parts-api
AWS_CLOUDWATCH_REGION_NAME=ap-southeast-2
# Note: Uses the same AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY as S3
# Ensure your AWS IAM user/role has CloudWatch Logs permissions:
# - logs:CreateLogGroup
# - logs:CreateLogStream
# - logs:PutLogEvents
# - logs:DescribeLogStreams

# WhatsApp Configuration (Twilio)
TWILIO_WHATSAPP_ENABLED=False
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
# Twilio Environment: 'sandbox' or 'production'
# - sandbox: For testing with Twilio WhatsApp Sandbox (default)
#   - Only works with numbers that have joined your sandbox
#   - Sandbox number: +14155238886
# - production: For production use with your approved WhatsApp Business number
#   - Requires WhatsApp Business API approval from Twilio
#   - Use your own WhatsApp Business number
TWILIO_ENVIRONMENT=sandbox
# WhatsApp From Number:
# - Sandbox: whatsapp:+14155238886 (Twilio's sandbox number)
# - Production: whatsapp:+1234567890 (Your approved WhatsApp Business number)
#   Replace with your actual WhatsApp Business number from Twilio
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886

